
  # High-fidelity mobile onboarding (Copy) (Copy)

  This is a code bundle for High-fidelity mobile onboarding (Copy) (Copy). The original project is available at https://www.figma.com/design/31r5tLMtwM1V4Lzjb0znZw/High-fidelity-mobile-onboarding--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  